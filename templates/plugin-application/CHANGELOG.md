# Changelog

All notable changes to these packages are documented here. The format follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/); both packages adhere to
[Semantic Versioning](https://semver.org/spec/v2.0.0.html).

Releases are cut as git tags `routes/v<version>` and `app/v<version>` (see README).

## [Unreleased]

## [0.1.0] - 1970-01-01

### Added
- `PACKAGE_ID-routes`: schema (`GET`) and submit (`POST`) HTTP-handler routes.
- `PACKAGE_ID-app`: portal registration at `/apps/PACKAGE_ID`.
