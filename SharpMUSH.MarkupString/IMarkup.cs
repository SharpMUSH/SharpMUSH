namespace MarkupString;

/// <summary>
/// Marker interface for a single markup layer (e.g. a color, a style, a link). Implementations
/// are expected to be records (or record structs) so equality is by value.
/// </summary>
public interface IMarkup
{
}
