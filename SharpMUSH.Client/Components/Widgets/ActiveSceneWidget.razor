@using System.Text.Json

@* TODO(Phase 5): show the most recent active scene from the server scene API.
   The client-only fake (InMemorySceneService) was removed in Phase 1. *@
<MudPaper Elevation="0" Style="padding:20px;border-radius:var(--radius);border:1px solid var(--border);background:var(--surface);height:100%;box-sizing:border-box;">
    <MudText Style="color:var(--text-dim);">The active-scene widget is being rebuilt on the new scene engine.</MudText>
</MudPaper>

@code {
    [Parameter] public JsonElement? Config { get; set; }
    [Parameter] public string Zone { get; set; } = string.Empty;
}
