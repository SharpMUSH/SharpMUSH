@page "/characters"
@using SharpMUSH.Client.Components.Widgets
@inject IStringLocalizer<SharedResource> Loc

<PageTitle>@Loc["Characters"]</PageTitle>

<MudContainer MaxWidth="MaxWidth.Large" Class="mt-4">
    @* The directory is a registered portal widget (CharacterDirectory), so it can also be
       dropped into any content zone via the layout editor. Who is listed is the game's call:
       the FN`CHARVIS softcode filters MUSH-side (guests hidden by default). *@
    <CharacterDirectoryWidget />
</MudContainer>
