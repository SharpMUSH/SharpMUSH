@page "/register"
@inject NavigationManager Nav

@* Registration is now integrated into the Login page (Register tab). *@
@code {
    protected override void OnInitialized()
        => Nav.NavigateTo("/login", replace: true);
}
