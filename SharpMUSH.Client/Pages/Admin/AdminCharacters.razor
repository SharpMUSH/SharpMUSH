@page "/admin/characters"
@attribute [Authorize(Policy = "players.view")]
@inject IStringLocalizer<SharedResource> Loc

<PageTitle>Characters — Admin — SharpMUSH</PageTitle>

<div style="padding:var(--cpad);">
    <div class="ph-header">
        <div class="ph-kicker">Administration</div>
        <h1 class="ph-title">Characters</h1>
    </div>

    <div class="ph-empty">
        <MudIcon Icon="@Icons.Material.Filled.Person" Class="ph-empty-icon" />
        <div class="ph-empty-text">Character administration coming soon.</div>
    </div>
</div>
