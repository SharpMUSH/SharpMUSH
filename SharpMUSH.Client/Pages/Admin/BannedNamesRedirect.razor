@page "/admin/bannednames"
@inject NavigationManager NavigationManager

@code {
    protected override void OnInitialized()
    {
        // Redirect to new config URL
        NavigationManager.NavigateTo("/admin/config/bannednames", replace: true);
    }
}
