# hello-world

The smallest possible package: a single thing carrying one `$command`
attribute, written in the shorthand form (`ATTR: "value"` instead of a
`value:`/`flags:` mapping).

After install, players get a `+hello` command that greets them by name.
